[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"

# Official Next.js runtime for Netlify (App Router, SSR, API routes, image optim).
[[plugins]]
  package = "@netlify/plugin-nextjs"
